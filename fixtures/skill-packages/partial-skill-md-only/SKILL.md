---
name: partial-package-skill
description: Use when only the package entry point was supplied and sibling files may be missing.
---

# Partial Package Skill

This entry point mentions additional references and scripts that may exist in a full package.

Load `references/details.md` when deeper rules are needed.
Use `scripts/analyze.mjs` only after confirming the package root and permission profile.
